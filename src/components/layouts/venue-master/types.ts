export type VenueFilters = {
  venueId?: string;
  venueName?: string;
  type?: string;
  location?: string;
  projectCode?: string;
  contact?:string;
  isActive?: boolean;
  showArchived?: boolean;
};